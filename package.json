{
  "name": "pocketpal-mac-lab",
  "version": "0.3.0",
  "private": true,
  "description": "Local Mac test harness; no npm dependencies required",
  "scripts": {
    "dev": "python3 tools/mac_server.py --preview --port 4173 --data-dir .test-data",
    "test": "python3 -m unittest discover -s tests -p 'test_mac_lab.py' -v"
  }
}
